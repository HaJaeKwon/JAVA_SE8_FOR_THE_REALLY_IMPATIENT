package Chapter03;

import org.junit.Test;

public class Question04 {

    /**
     * 자바 API에서 이름에 Filter를 포함하는 함수형 인터페이스를 몇 개나 찾을 수 있는가?
     * 이 중 어느 것이 Predicate<T> 보다 가치가 있는가?
     */
    @Test
    public void solution() {
    }

    /**
     * Java docs API 에서 FileFilter, DirectoryFilter, EventFilter 등 을 찾을 수 있다.
     * 이들 함수형 인터페이스는 Predicate<T> 로써 대체될 수 있다
     * 다만 받아야 하는 파라미터를 쓰임새에 맞게 제한함으로써 그 의미를 가진다
     */
}
