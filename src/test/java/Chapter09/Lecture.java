package Chapter09;

/**
 * Created by hajaekwon on 2019-04-26.
 */
public class Lecture {

    /**
     * 일반적으로, 이전 같으면 a.equals(b)를 호출했을 상황에
     * Objects.equals(a,b)를 호출하는 것이 좋다
     */

    /**
     * 예전부터 toString 대신 String.valueOf(obj)를 호출하는 널 안전 호출 방법이 있었다.
     * null이면 "null"을 리턴해준다
     * 이 방법이 마음에 들지 않으면 Objects.toString(obj, "") 처럼 null 인 경우 사용할 값을 전달할 수 있다
     */

}
